# LinkSites phase 3 — Template selection (LTS-103)

Stage ID: `linksites.template_selection`

`website_builder_bot` selects a template from the **LiNKsites external registry** (not invented in monorepo). Guidance: `template-guidance.md`.

Implementation: `suites/linksites/phases/template-selection/template-selection.ts`
