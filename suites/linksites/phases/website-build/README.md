# LinkSites phase 4 — Website build (LTS-104)

Stage ID: `linksites.website_build`

Business-specific copy, media, and style within the selected template. `website_builder_bot` produces `website_package`; handoff feeds `autowork.linksites.artifact_write_local`.

Implementation: `suites/linksites/phases/website-build/website-build.ts`
