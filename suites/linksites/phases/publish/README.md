# LinkSites phase 5 — Publish (LTS-105)

Stage ID: `linksites.publish`

Content synced to Payload CMS with temp URL `businessname.linktrend.media`. Requires `autowork.linksites.payload_sync_local` (LTS-032) and `preview_readiness_check` pass (LTS-033).

Implementation: `suites/linksites/phases/publish/publish.ts`
