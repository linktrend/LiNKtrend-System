# Cursor agents (relocated)

Specialist agent definitions moved to **`dev-swarm/agents/`**.

- Use `dev-swarm/agents/architect-integrator.md` for integration and wave orchestration.
- Use `dev-swarm/agents/project-planner.md` for planning packets.
- See `dev-swarm/AGENTS.md` for the full portable brief.

Legacy copies are archived at `dev-swarm/archive/cursor-agents-legacy/`.
