# Host skills

Skills for **this repository only**. Virgin Dev Swarm template ships this folder **empty** (except this README).

Add skills when this product needs stack- or domain-specific guidance not covered by `../gstack/`.

On conflict with gstack: **host wins** (see `dev-swarm/factory/install/SKILLS-ALLOWLIST.md`).

LiNKtrend-System: populated from migration (formerly `skills/linktrend/`).
