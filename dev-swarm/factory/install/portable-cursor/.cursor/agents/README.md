# Cursor agents (relocated)

Specialist agent definitions moved to **`dev-swarm/factory/agents/`**.

- Use `dev-swarm/factory/agents/architect-integrator.md` for integration and wave orchestration.
- Use `dev-swarm/factory/agents/project-planner.md` for planning packets.
- See `dev-swarm/AGENTS.md` for the full portable brief.

Legacy copies are archived at `dev-swarm/archive/cursor-agents-legacy/`.
