# Bootstrap launchers (one-line)

Paste into a Cursor or Codex agent. Branch `issue/<id>-<slug>` from `development`; verify clean `git status` before edit. No worktrees.

```
Execute dev-swarm/factory/programs/bootstrap/prompts/DS-001.prompt.md exactly. Update dev-swarm/factory/reports/bootstrap/DS-001.md before stopping.
```

Replace `DS-001` with the issue id. See `PROGRAM.md` for wave order.
