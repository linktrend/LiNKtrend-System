# Product programs

Empty in virgin template. After Chairman **Go**, Planner creates:

`dev-swarm/product/programs/<program-id>/PROGRAM.md`

and nested `modules/<module>/phases/<phase>/issues/`.

Bootstrap factory history lives in `dev-swarm/factory/programs/bootstrap/` (not here).
