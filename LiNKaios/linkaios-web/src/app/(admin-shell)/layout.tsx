/** Admin route group — auth required via middleware; surface picker on landing for now. */
export default function AdminShellRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
