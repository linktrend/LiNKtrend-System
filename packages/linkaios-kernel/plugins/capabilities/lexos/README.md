# LEXOS capability manifests (moved)

Canonical manifests: **`LiNKskills/capability-connectors/lexos/`**

This kernel directory is intentionally empty after Tier 2 consolidation. Loaders and catalog seeds must reference the LinkSkills paths.
