# LinkSkills Governance Engine

The governance engine owns lease lifecycle, policy modes, idempotency, safety checks, kill switches, capability catalog access, disclosure, and execution audit.

Current executable code remains in `LiNKskills/services/logic-engine` for package compatibility. Future extraction should move code here only when package imports and tests can be migrated together.
