# services

Cross-cutting support that is not a top-level `apps/*` product. Currently contains **Postgres migrations** under `migrations/` for the LiNKtrend-AdminDB Supabase project (per-service schemas).
