# services

Cross-cutting support that is not owned by one deployable subsystem. Currently contains **Postgres migrations** under `migrations/` for the LiNKtrend-AdminDB Supabase project and per-service schemas.
