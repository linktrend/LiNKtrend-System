# Report: LTS-034

_Status: verified — CRM ready_to_contact gated on preview pass_

**Principal ref:** PPD §5 outreach gate

## Summary

`autowork.linksites.crm_ready_to_contact_mark` requires lease, boolean `checks_passed === true`, and valid `check_report_ref`. Fails when preview not passed — outreach gate does not silently skip.

## Proof

```json
{
  "issue_id": "LTS-034",
  "acceptance_criteria": [
    {
      "id": "AC1",
      "text": "CRM shadow/mock lead status updates only after preview pass",
      "verified": true,
      "evidence": ["linksites-v2.ts createCrmReadyToContactMarkHandler", "linksites-v2.test.ts crm gate tests"]
    },
    {
      "id": "AC2",
      "text": "Outreach gate records audit when held for approval",
      "verified": true,
      "evidence": ["outreach.held_for_approval audit in mission.ts executeOutreachDraft"]
    }
  ]
}
```
