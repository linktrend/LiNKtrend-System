# Report: LTS-001

**Status:** complete  
**Principal ref:** PPD §5 infrastructure; §3 LiNKaios  
**Branch:** `issue/LTS-001-supabase-kernel-spine`

## Summary

Delivered Supabase MVO spine migrations under `supabase/migrations/`, tenant-scoped RLS exposure for kernel/brain/skills, Project→Run persistence (`linkaios.create_project`, `linkaios_kernel.create_run` with `project_id`), and LiNKaios APIs (`POST /api/projects`, `GET /api/projects/[projectId]/runs`). GSM secret placeholders added to `.env.example` only.

## Acceptance criteria

| Criterion | Result |
|-----------|--------|
| Supabase migrations for kernel/brain/skills exposure with tenant_id RLS | `supabase/migrations/202606010001_*.sql` |
| Project and Run APIs persist and join traces per Suite terminology | RPC + routes above |
| GSM secret placeholders in `.env.example` only | Updated root `.env.example` |

## Commands

```bash
cd LiNKaios/linkaios-web && npm run typecheck
cd LiNKaios/linkaios-web && npm test -- src/lib/projects/
LINKDEV_SCOPE='supabase/migrations LiNKaios .env.example' LiNKdev/factory/scripts/verify.sh
LINKDEV_SCOPE='supabase/migrations LiNKaios .env.example' LINKDEV_REPORT=LiNKdev/product/reports/linktrend-system/linkaios/kernel/LTS-001.md LiNKdev/factory/scripts/run-gates.sh --tier A --scope 'supabase/migrations LiNKaios .env.example' --report LiNKdev/product/reports/linktrend-system/linkaios/kernel/LTS-001.md --program linktrend-system
```

```json
{
  "issue_id": "LTS-001",
  "acceptance_criteria": [
    {
      "id": "migrations_rls",
      "text": "Supabase migrations applied for kernel/brain/skills exposure with tenant_id RLS",
      "verified": true
    },
    {
      "id": "project_run_api",
      "text": "Project and Run APIs persist and join traces per Suite terminology",
      "verified": true
    },
    {
      "id": "gsm_placeholders",
      "text": "GSM secret placeholders documented in .env.example only",
      "verified": true
    }
  ],
  "commands": [
    "LiNKaios/linkaios-web: npm run typecheck",
    "LiNKaios/linkaios-web: npm test -- src/lib/projects/",
    "LiNKdev/factory/scripts/verify.sh (issue scope)"
  ]
}
```
