# Report: LTS-002

**Status:** complete  
**Principal ref:** PPD §4 Client; §5 MVO surfaces  
**Branch:** `issue/LTS-002-client-mvo-flow`

## Summary

Wired the Client MVO operator path: sign-in route, LinkSites subscribe surface, project launch wizard (`/projects/new?suite=linksites`), and post-launch Run progress entry via `ProjectCreatedBanner` linking to the Runs tab.

## Acceptance criteria

| Criterion | Result |
|-----------|--------|
| Licensee can sign in and enable LinkSites Suite | `/login` + `/suites/linksites?tab=subscribe` (`client-mvo-flow.ts`) |
| Operator can launch a Project for one lead | `NewProjectWizard` POST `/api/projects` with suite preset |
| Project detail shows run progress entry points | `ProjectCreatedBanner` → `?tab=runs` on project detail |

## Commands

```bash
cd LiNKaios/linkaios-web && npm run typecheck
cd LiNKaios/linkaios-web && npm test -- src/lib/client-mvo-flow.test.ts
LINKDEV_SCOPE='LiNKaios/linkaios-web' LiNKdev/factory/scripts/verify.sh
```
