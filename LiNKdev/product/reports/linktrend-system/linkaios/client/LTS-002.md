# Report: LTS-002

**Status:** complete  
**Principal ref:** PPD §4 Client; §5 MVO surfaces  
**Branch:** `issue/LTS-002-client-mvo-flow`

## Summary

Wired the Client MVO operator path: sign-in route, LinkSites subscribe surface, project launch wizard (`/projects/new?suite=linksites`), and post-launch Run progress entry via `ProjectCreatedBanner` linking to the Runs tab.

## Acceptance criteria

| Criterion | Result |
|-----------|--------|
| Licensee can sign in and enable LinkSites Suite | `/login` + `/suites/linksites?tab=subscribe` (`client-mvo-flow.ts`) |
| Operator can launch a Project for one lead | `NewProjectWizard` POST `/api/projects` with suite preset |
| Project detail shows run progress entry points | `ProjectCreatedBanner` → `?tab=runs` on project detail |

## Commands

```bash
cd LiNKaios/linkaios-web && npm run typecheck
cd LiNKaios/linkaios-web && npm test -- src/lib/client-mvo-flow.test.ts
LINKDEV_SCOPE='LiNKaios/linkaios-web' LiNKdev/factory/scripts/verify.sh
```

## Proof

```json
{
  "issue_id": "LTS-002",
  "branch": "issue/LTS-002-client-mvo-flow",
  "merge_commit": "4915bb05e36b9daaed1bccc1541402cd8874dda6",
  "acceptance_criteria": [
    {
      "id": "client-sign-in-suite-subscribe",
      "description": "Licensee can sign in and enable LinkSites Suite",
      "verified": true,
      "evidence": "/login and /suites/linksites?tab=subscribe are documented in client-mvo-flow.ts and covered by client-mvo-flow.test.ts"
    },
    {
      "id": "project-launch-for-lead",
      "description": "Operator can launch a Project for one lead",
      "verified": true,
      "evidence": "NewProjectWizard submits the LinkSites suite preset to POST /api/projects"
    },
    {
      "id": "run-progress-entry-point",
      "description": "Project detail shows run progress entry points",
      "verified": true,
      "evidence": "ProjectCreatedBanner links successful launches to the project detail Runs tab"
    }
  ],
  "commands": [
    {
      "command": "cd LiNKaios/linkaios-web && npm run typecheck",
      "status": "passed in executor report"
    },
    {
      "command": "cd LiNKaios/linkaios-web && npm test -- src/lib/client-mvo-flow.test.ts",
      "status": "passed in executor report"
    },
    {
      "command": "LINKDEV_SCOPE='LiNKaios/linkaios-web' LiNKdev/factory/scripts/verify.sh",
      "status": "passed in executor report; integrator rerun required a follow-up scanner fixture cleanup on development"
    }
  ]
}
```
