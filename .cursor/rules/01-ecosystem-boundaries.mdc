---
description: Service-boundary rules for the LiNKtrend ecosystem.
alwaysApply: true
---

# Ecosystem Boundaries

LiNKaios is the organizational execution control plane. It owns tenant/plugin/work/role/bot orchestration, UI, dashboards, work contracts, routing, approvals, and system-level governance.

LiNKbrain is the institutional memory and learning plane. It owns the event ledger, memory objects, audit union, context assembly, retrieval, and benchmark/learning pipelines.

LinkSkills is the capability governance plane. It owns capability catalog, capability leases, side-effect permissions, approvals, run ledger, idempotency, kill switches, and skill certification.

LiNKautowork is the deterministic workflow execution plane. It owns n8n gateway, workflow templates, workflow runs, deterministic execution audit, and workflow promotion candidates.

LinkBot is the role-bound AI employee runtime shell. It owns persona/session/reasoning, but does not own canonical memory, canonical skills, secrets, deterministic workflows, or final audit.

No service should absorb another service's responsibility.

Vertical plugins describe business workflows. Capability plugins provide reusable software/tool connections. LinkBots perform judgment work. LiNKautowork performs deterministic automation. LinkSkills grants permissions and skills. LiNKbrain remembers. LiNKaios coordinates.

If an agent does not know the intended workflow for a vertical plugin, capability workflow, or vertical-capability combination, it must stop and ask before implementing. Do not invent business workflows, software schemas, chart-of-accounts, CRM stages, Payload content models, Odoo records, or similar domain configuration based on assumptions.

When preparing a capability plugin, build only the governed connector surface: configuration, authentication boundary, capability contract, lease/audit hooks, idempotency, mode flags, and communication path between the external software and LiNKaios, a vertical plugin, or a LinkBot. Do not define the internal business setup of the connected software unless explicitly assigned. Prefer copying/adapting existing completed schemas or configs from the source repo when the user says they already exist.

When in doubt, route through contracts:

- memory/context/audit → LiNKbrain
- capability/action permission → LinkSkills
- deterministic workflow → LiNKautowork
- reasoning/session/channel → LinkBot
- tenant/work/plugin/dashboard → LiNKaios
